网络帧:
    1.服务器开启一个循环线程,间隔时间不定
    2.执行事件:
        -- 
        -- 将玩家上一网络帧的操作同步出去
        -- 利用玩家的


帧同步过程:

-- 服务器发送初始状态给客户端
-- 开启一个循环线程,每间隔一定时间执行一次网络帧
    1.对比玩家上传的帧状态和服务器存储的帧状态
    2.将玩家当前的帧操作同步出去
    3.服务器本地执行一次帧操作
-- 客户端接受到服务器的网络帧
    1.执行携带的帧操作
    2.将当前的帧操作上传给服务器
    3.将当前的帧状态上传给服务器

